import {
    add,
    arrowDown,
    close,
    search,
    interrogation
} from './iconsText';

export const iconsPaths: Record<string, string> = {
    add,
    'arrow-down': arrowDown,
    close,
    search,
    interrogation
}