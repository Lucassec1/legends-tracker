import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { moduleMetadata } from "@storybook/angular";
import { CommonModule } from "@angular/common";
import { ButtonComponent } from "../src/app/components/button/button.component";

<Meta title="Components/Buttons" component={ButtonComponent} />

# Button

O projeto conta com um botão principal que é o primary, mas existe 3 tipos de botões:

-`primary` -`secondary` -`ghost`

<Canvas>
    <Story
        name="Primary"
        decorators={[
            moduleMetadata({
                declarations: [
                    ButtonComponent
                ],
                imports: [CommonModule],
            }),
        ]}
    >
        {{
        template: `<app-button label="Primary"></app-button>`,
        }}
    </Story>
</Canvas>
