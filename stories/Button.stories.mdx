import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { ButtonComponent } from "../src/app/components/button/button.component.ts";
import { moduleMetadata } from "@storybook/angular";
import { CommonModule } from "@angular/common";

<Meta title="Lib/Buttons/button" />

# Button

O projeto conta com um botão principal que é o `primary`, mas no contexto geral existem 3 tipos:

<Canvas>
  <Story 
    name="type: primary"
    decorators={[
        moduleMetadata({
            declarations: [
                ButtonComponent
            ],
            imports: [CommonModule]
        })
    ]}>

    {{ template: `<app-button label="Primary"></app-button>` }}

  </Story>

</Canvas>
